# Offline relevance/quality scoring — no LLM/token APIs.
# For a CPU-only install, pre-install torch from the CPU index to keep it small:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
numpy>=1.26
scikit-learn>=1.4
rank-bm25>=0.2.2
PyYAML>=6.0
sentence-transformers>=3.0
